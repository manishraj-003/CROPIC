# AI Services Scaffold

## Modules
- `classification.py`: baseline crop/stage predictor stub
- `segmentation.py`: damage ratio computation from segmentation masks
- `tamper.py`: ELA helper placeholder

## Run tests
```powershell
cd ai-services
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
pytest
```
